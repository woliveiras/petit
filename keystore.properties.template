# Template - copy to keystore.properties and fill in with your credentials
# NEVER commit the keystore.properties file to git! It contains sensitive information that should not be shared publicly.

storeFile=petit-release-key.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=petit
keyPassword=YOUR_STORE_PASSWORD
